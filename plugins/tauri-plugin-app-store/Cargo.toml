[package]
name = "tauri-plugin-app-store"
version = "0.1.0"
authors = ["Reflect"]
description = "Reports which channel installed the Reflect iOS app (App Store, TestFlight sandbox, or Xcode) via StoreKit 2's AppTransaction."
edition = "2021"
rust-version = "1.77.2"
links = "tauri-plugin-app-store"

[dependencies]
tauri = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[build-dependencies]
tauri-plugin = { workspace = true }
