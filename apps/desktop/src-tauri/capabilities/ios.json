{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "ios",
  "description": "iOS-only capabilities: the StoreKit In-App Purchase bridge and the App Store environment probe",
  "platforms": ["iOS"],
  "windows": ["main"],
  "permissions": ["iap:default", "app-store:default"]
}
