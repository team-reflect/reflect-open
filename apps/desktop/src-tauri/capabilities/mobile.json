{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Mobile-only capabilities: the light impact haptic and the native audio-memo recorder. Keyboard webview tuning is command-less and needs no permission.",
  "platforms": ["iOS", "android"],
  "windows": ["main"],
  "permissions": ["mobile-haptics:default", "recording:default"]
}
