{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Mobile-only capabilities: keyboard webview tuning (Plan 19), the light impact haptic, and the native audio-memo recorder",
  "platforms": ["iOS", "android"],
  "windows": ["main"],
  "permissions": ["keyboard:default", "mobile-haptics:default", "recording:default"]
}
